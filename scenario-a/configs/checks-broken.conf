# Task 10 break test #2: a host that does not resolve at all, plus a port with
# nothing listening, plus a malformed line. The script must report all three and
# exit 1 -- never hang, never crash.
app-1|http://127.0.0.1:3101/healthz|200
bad-dns|http://doesnotexist.invalid/|200
bad-port|http://127.0.0.1:59999/healthz|200
this-line-is-malformed
